server:
  port: 8888
  connection-timeout: 86400000

logging:
  path: logs

oracle:
  driver: oracle.jdbc.driver.OracleDriver
  url: jdbc:oracle:thin:@localhost:1521:xe
  username: fullsample
  password: fullsample
  threshold: 500

excel:
  version: 2016

esAgent:
  schemaIdUrl: http://192.168.1.221:3006/excel/schema-fields/{schemaId}
  importedLogServiceUrl: http://localhost:3006/imported/log

spring:
  http:
    multipart:
      enabled: true
      max-file-size: 100Mb
      max-request-size: 100Mb
